DB_HOST=localhost                # MySQL host (use 'localhost' or your MySQL server address)
DB_USER=root                     # MySQL username (e.g., 'root')
DB_PASSWORD=shubham           # MySQL password (update with your actual password)
DB_DATABASE=dairyopss            # The name of your MySQL database (update accordingly)
JWT_SECRET=jwt_secret            # Secret for JWT authentication (keep it secure)
PORT=8000                        # Port number for your server
EMAIL=amineshsolanki2@gmail.com  # Email address (used for email-related functionality)
EMAIL_PASSWORD=vmiz dlfc vobn qezi # Email password (use environment-specific settings)
BASE_URL=http://localhost:8000   # The base URL for your backend API
FRONTEND_URL=http://localhost:5173 # The URL for your frontend (e.g., React or Vue app)
